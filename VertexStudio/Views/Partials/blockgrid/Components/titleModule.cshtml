@* DEPRECATED: o alias real do doctype é "title", não "titleModule".
   O dispatcher (blockgrid/items.cshtml) usa item.Content.ContentType.Alias,
   por isso este ficheiro nunca é chamado. Ver Components/title.cshtml. *@